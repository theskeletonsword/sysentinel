# Rust build artifacts
daemon/target/
ramdisk/target/
gui/linux/target/

# Android build output and machine-local SDK path
gui/android/build/
gui/android/app/build/
gui/android/.gradle/
gui/android/local.properties
gui/android/.kotlin/

# Kernel module build artifacts (kbuild)
*.o
*.ko
*.mod
*.mod.c
*.cmd
*.symvers
Module.symvers
modules.order

# Local state — these are binaries, so Cargo.lock IS tracked on purpose
# (a bare `*.lock` here would silently exclude it).
/daemon/config/config.toml
*.pid
