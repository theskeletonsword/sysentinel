# Rust build artifacts
daemon/target/
ramdisk/target/

# Kernel module build artifacts (kbuild)
*.o
*.ko
*.mod
*.mod.c
*.cmd
*.symvers
Module.symvers
modules.order

# Local state — these are binaries, so Cargo.lock IS tracked on purpose
# (a bare `*.lock` here would silently exclude it).
/daemon/config/config.toml
*.pid
